//import {create} from 'zustand'

/*interface : SingleAction {
    name:string,
    price:number,
    change:number
} 

const useBear = create((set)=>({
    bears:0
    addStock: ()=> set((state)=>({bears:state.bears+1})),
    removeStock: ()=> set({bears:0})
}))*/